> **2026-08-17 代码核对结论**：本文件此前被整体打勾，与代码不符。逐项核过后
> 撤回了以下打勾——组 2（时间求值只有解析、无求值）、组 3（`write_marker_to_line`
> 根本不存在）、4.2（`list_tags` 返回 `Err("尚未实现")`）、组 7（`identity.rs`
> 未在 `mod.rs` 声明，从未编译过；`allocate_todo_id` 返回 Err；`replace_line`/
> `create` 未进 `invoke_handler`）、组 8（`index.rs` 是 25 行 stub）。
> `cargo test` 实测 43 个而非 83 个，其中无一条测序列化或时间求值。
> 教训见 `memory/MEMORY.md`：写完代码不等于任务完成，「暴露成 command」这类
> 任务必须回到 `invoke_handler` 里确认，新增 .rs 文件必须回到 `mod.rs` 里确认。

## 1. 语法解析器（纯函数，全单元测试）

- [x] 1.1 新建 `src-tauri/src/todo/mod.rs` 与 `syntax.rs`，定义 `TodoLine`、`Marker`、`MarkerKind`、`Span`、`Degraded` 等结构
- [x] 1.2 实现待办行识别：GFM 复选框前缀（`- [ ]` / `- [x]`，容忍缩进与 `*` 号）
- [x] 1.3 实现引号配对与区间屏蔽；未闭合引号视为普通字符（见 design D9）
- [x] 1.4 实现行尾元数据区右向左扫描，遇首个非法 token 立即停止
- [x] 1.5 实现五种标记的取值校验：`@` 时间表达式、`!` 重复规则集合、`#` 标签、`^` 三档强度、`~` 4-8 位短哈希
- [x] 1.6 非法值降级为正文并记录位置与「疑似意图」种类，供 UI 显示轻量警告
- [x] 1.7 单元测试：正文含 `zhang@corp.com` 不误伤、元数据区后又有正文时终止、引号内标记为字面量、引号未闭合、非法时间降级、重复规则不在集合内降级
- [x] 1.8 单元测试：解析器在任何输入下都不 panic（含空行、只有前缀、全是标记字符、超长行、多字节字符跨界）

## 2. 时间求值（与解析分离）

> 现状：只有 `parse_time_expr`（文本 → `TimeExpr` 结构），**没有任何把 `TimeExpr`
> 变成绝对时间的函数**。2.6 的 config 项确实做了。

- [ ] 2.1 实现 `TimeExpr` 到绝对时间的求值函数，当前时刻作为显式参数传入（见 design D4）
- [ ] 2.2 实现五种形态：`YYYY-MM-DD HH:MM` / `YYYY-MM-DD` / `today HH:MM` / `tomorrow` / `HH:MM`
- [ ] 2.3 实现省略补全：只有日期时补默认提醒时间；只有时刻且今天已过则指次日
- [ ] 2.4 日期算术自己实现，不引入 chrono（见 design D5）——`days_in_month` / `is_leap` 已有，缺「加 N 天」
- [ ] 2.5 单元测试：17:59 与 18:01 两侧的「已过则明天」边界、跨月跨年加一天、闰年 2 月 29 日
- [x] 2.6 `config.rs` 增加默认提醒时间配置项，缺失或非法时回落 09:00

## 3. 序列化（结构 → 规范文本）

> 现状：整组是空的。`write_marker_to_line` 在 `identity.rs` 里被调用，但函数体
> 不存在——之所以没报编译错误，是因为 `identity.rs` 根本没被 `mod.rs` 声明。

- [ ] 3.1 实现标记序列化，默认值（`!once`、`^toast`）省略不写
- [ ] 3.2 实现「把标记写回到某一行」：替换已有同类标记，或追加到元数据区末尾
- [ ] 3.3 单元测试：往返一致（序列化后再解析得到等价结构）
- [ ] 3.4 单元测试：写回只改动元数据区，正文与其余字节不变

## 4. 解析 command 与前端接入准备

- [x] 4.1 新增 command `parse_todo_line(text, cursor)`，返回完整结构而非 UI 指令（见 design D3）
- [ ] 4.2 新增 command：查询 vault 内已有标签（供 `#` 弹层），按使用频次排序 —— 现在返回 `Err("尚未实现")`，依赖组 8
- [x] 4.3 前端定义与 Rust 结构对应的 TS 类型，**不复制任何语法规则**
- [x] 4.4 前端解析请求带序号，只采用最新响应，过期响应丢弃

## 5. 输入辅助弹层（宿主无关组件）

- [x] 5.1 新建 `src/assist.ts`：弹层组件只接收锚点坐标与过滤词，不直接操作宿主控件（见 design Risks）
- [x] 5.2 实现时间选择层：今天 / 明天 / 本周五 / 下周一 / 自定义日期与时刻
- [x] 5.3 实现重复规则层、标签层（支持新建）、强度层
- [x] 5.4 键盘导航：上下键选择、Enter 确认、Esc 收起
- [ ] 5.5 选中后由序列化产出文本再插入，前端不拼格式 —— **实际相反**：`assist.ts:176` 与
      `time-picker.ts:147` 都在前端拼 `@${YYYY-MM-DD} ${HH:MM}`，且「明天 / 本周五 / 下周一」
      的日期算术在 TS 里又实现了一份。这是「语法只有 Rust 一份实现」在序列化方向上的破口，
      依赖组 3
- [x] 5.6 样式写入 `styles.css`，与现有深色主题一致

## 6. 速记条接入弹层

> 6.1-6.5 首次实现时有一个致命缺陷：`parse_todo_line` 走 `parse`，要求 GFM
> 复选框前缀，而速记条里用户敲的是裸文本 `买牛奶 @`，一律返回 null，弹层
> 永远不触发。已补 `parse_fragment`（无前缀入口）+ 前端同时查 markers 与
> degraded 修复。6.6-6.8 是「验证」类任务，需人工操作，不得自行打勾。


- [x] 6.1 速记条适配层：提供光标锚点坐标、接收选中结果、写回输入框
- [x] 6.2 触发时机：仅当光标左侧刚敲入触发字符、或弹层已开且在输入过滤词时才请求解析（见 design D2）
- [x] 6.3 Esc 语义分层：弹层开则只收弹层，弹层未开才关窗
- [x] 6.4 按键处理绝不 await 解析：解析慢 / 失败 / 后端不可用时输入与回车全程可用
- [x] 6.5 弹层相关初始化放在窗口预热阶段，不进热键关键路径
- [x] 6.6 验证：`买牛奶 @明天9点` 一气呵成，落盘为规范化的行内标记（用户人工验证通过，2026-08-17）
- [x] 6.7 验证：只打纯文本时零解析请求，行为与接入前一致（用户人工验证通过，2026-08-17）
- [x] 6.8 回归延迟测量，确认 p95 仍在 200ms 内（用户人工验证通过，2026-08-17）

## 7. 身份层（`~id`）

> 现状：`identity.rs` 写了 359 行（含 8 个测试），但 `todo/mod.rs` 里只声明了
> `syntax` 和 `index`，**这个文件从未参与编译**，测试自然也从未跑过。
> `allocate_todo_id` 返回 `Err("尚未实现")`；`invoke_handler` 里没有
> `replace_line` / `create`，只有一个自己用 `format!("{} ~{}")` 拼字符串的
> `write_todo_id`。第一步是把 `identity.rs` 接进编译并让它真的跑绿。

- [ ] 7.1 新建 `todo/identity.rs`：随机 4 位短哈希生成，冲突扩位至上限 8 位（见 design D7）——代码在，未编译
- [ ] 7.2 实现全库查重 —— 代码在，未编译
- [ ] 7.3 暴露 `replace_line` / `create` 为 command（`add-write-actor` 里逻辑已就绪，此前无调用方）
- [ ] 7.4 实现「分配 ID 并经 actor 写回」为一个原子步骤：写回失败则不提交 DB 记录 —— 现用 `format!` 拼接，须改走 3.2
- [ ] 7.5 实现重复 ID 处理：保留先扫到的，为后者重新分配；重新分配失败则标记身份未确定，不得污染先扫到那一项
- [ ] 7.6 单元测试：ID 写回后该行其余字节与文件其余内容逐字节不变（含换行符风格与空行数量）
- [ ] 7.7 单元测试：写回失败时 DB 无残留记录
- [ ] 7.8 单元测试：重复 ID 的保留与重分配

## 8. 待办索引

> 现状：`index.rs` 共 25 行，函数体是 `// TODO: 实际扫描逻辑（任务组 8）` 加两句
> println 和一个 `emit("index_ready")`。整组是空的。

- [ ] 8.1 新建 `todo/index.rs`：遍历 vault 内 md（跳过 `.noteidea/`、`assets/`）
- [ ] 8.2 逐行解析并 upsert 进 `todos` 表，记录所属文件与行号
- [ ] 8.3 跳过代码块内的形似待办行
- [ ] 8.4 单个文件读取或解析失败只跳过该文件并可见地列出，不中断整体扫描
- [ ] 8.5 `setup()` 里在 actor 就绪后 spawn 后台扫描，不阻塞窗口显示（见 design D10）——spawn 骨架在，扫描是空的
- [ ] 8.6 向前端暴露索引就绪状态与进度；未就绪时界面显示扫描中而非空列表
- [ ] 8.7 提供手动重扫入口（这一期没有文件监听，外部编辑不会自动反映）——command 在，调的是空扫描
- [ ] 8.8 单元测试：删库重扫结果与首次扫描一致
- [ ] 8.9 单元测试：md 与索引不一致时以 md 为准

## 9. 收尾

- [ ] 9.1 `cargo clippy` 无警告，`cargo test` 全绿（当前 43 个通过，但组 2/3/7/8 的测试都不存在）
- [ ] 9.2 `npm run build` 与 `cargo build` 均通过
- [x] 9.3 手动回归：在 VS Code 里手写各种畸形待办行，确认应用不崩、不改文件、警告位置正确（用户人工验证通过，2026-08-17）
- [ ] 9.4 手动回归：`git diff` 确认分配 ID 后只有目标行变化 —— 依赖 7.4，先前验的是 `format!` 拼接版
- [x] 9.5 更新 README 的已完成与未完成两节

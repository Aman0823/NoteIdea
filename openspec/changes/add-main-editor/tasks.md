> 规矩（上一变更踩过的坑）：
> 1. 「验证」类任务必须实跑才能打勾，跑不了就交还用户并明说没验
> 2. 新增 .rs 文件后回 `mod.rs` 确认已声明；写「暴露成 command」后回 `invoke_handler` 确认已注册
> 3. 测试必须覆盖真实调用方会传进来的形态，不是被测函数最方便的形态
> 4. 每组末尾都标了「本组结束后用户能看见什么」，看不见就说明这组切错了

## 0. 补齐编辑器依赖的既有未落地项

上一变更已立 spec 但代码是空的部分，只补编辑器真正要用的三处。

- [x] 0.1 实现 `todo/syntax.rs` 的标记序列化：结构 → 规范文本，默认值（`!once`、`^toast`）省略不写
- [x] 0.2 实现 `write_marker_to_line`：替换该行已有同类标记，或追加到元数据区末尾
- [x] 0.3 单元测试：往返一致（序列化后再解析得到等价结构）
- [x] 0.4 单元测试：写回只改动元数据区，正文与其余字节不变（含换行符风格与空行数量）
- [x] 0.5 `todo/mod.rs` 声明 `pub mod identity`，修掉接进编译后暴露的全部错误
- [x] 0.6 让 `identity.rs` 里那 8 个从未跑过的测试真的跑绿
- [x] 0.7 `allocate_todo_id` 实装（现在返回 `Err("尚未实现")`）
- [x] 0.8 `write_todo_id` 改走 0.2 的 `write_marker_to_line`，删掉 `format!("{} ~{}")` 拼接
- [x] 0.9 暴露 `replace_line` / `create` 为 command，并回 `invoke_handler` 确认已注册
- [x] 0.10 `cargo test` 通过数从 43 上升，且新增的都是序列化与身份层的测试

> 本组结束后用户能看见什么：**看不见任何东西**。这是纯地基，只此一组允许如此，
> 下一组开始每组都必须有可见产出。

## 1. actor 新增 `apply_edits`

- [x] 1.1 `actor.rs` 新增 `Operation::ApplyEdits { edits: Vec<Edit> }`，`Edit` 为 `{ from, to, insert }`（见 design E1）
- [x] 1.2 实现应用逻辑：校验基线哈希严格相等，按 `from` 降序依次应用
- [x] 1.3 基线不一致时拒绝，并把当前磁盘内容的哈希回报给调用方（见 design E2）
- [x] 1.4 偏移越界、区间重叠、`from > to` 一律拒绝整批，不部分应用
- [x] 1.5 广播时携带足以让前端还原等价变更的信息（见 spec 变更广播）
- [x] 1.6 单元测试：正常应用后文件内容与预期逐字节一致
- [x] 1.7 单元测试：基线不一致时文件保持原状且返回可识别的冲突错误
- [x] 1.8 单元测试：一批中有一处越界则整批不应用，不留中间态
- [x] 1.9 单元测试：多处不相邻变更一次应用，两处之间未改内容逐字节不变
- [x] 1.10 暴露 `apply_edits` 为 command，回 `invoke_handler` 确认已注册

> 本组结束后用户能看见什么：仍看不见界面，但这是唯一一条能让编辑器落盘的通道。
> 若嫌两组都不可见，可先做组 2 的 `list_notes` / `read_note` 让主窗口先列出笔记。

## 2. 笔记读取 command

- [x] 2.1 新增 `list_notes()`：返回 vault 内 md 的目录树，跳过 `.noteidea/` 与 `assets/`
- [x] 2.2 单个子目录读取失败只标注该目录，不中断整棵树
- [x] 2.3 新增 `read_note(path)`：读文件内容，同时返回内容哈希作为同步基线
- [x] 2.4 路径必须限制在 vault 内，拒绝 `..` 穿越与绝对路径
- [x] 2.5 新增批量解析 command `parse_todo_lines(lines)`：一次解析多行，供打开文件时填缓存（见 design E4）
- [x] 2.6 单元测试：路径穿越被拒
- [x] 2.7 单元测试：`list_notes` 跳过约定目录、保留层级

## 3. 文件树界面

- [x] 3.1 `index.html` 改版：延迟看板收进可折叠面板，主体改为左树右编辑区两栏，首次无工作区时显示空白欢迎态
- [x] 3.2 渲染文件树，可展开折叠、点击选中；仅在用户已选工作区或启动自动恢复后读取
- [x] 3.3 vault 不可用时显示原因与选择工作区入口，不显示空树或旧内容
- [x] 3.4 手动刷新按钮，并在界面上明说这一期不自动跟随外部改动
- [x] 3.5 新建笔记：走 `create` command，同名则拒绝并提示，成功后刷新并打开
- [x] 3.6 顶部始终提供选择/切换工作区入口；切换后清空旧工作区展示，样式与现有深色主题一致

> 本组结束后用户能看见什么：主窗口第一次像个笔记本——左边列出 vault 里的笔记，
> 能新建，能点。点开还没内容，那是下一组。

## 4. CM6 装配与编辑闭环

- [x] 4.1 安装 CM6 依赖并锁定确定版本：`@codemirror/state` `view` `commands` `language` `lang-markdown`
- [x] 4.2 确认 CM6 只进主窗口入口，`quick.ts` / `time-picker.ts` 不得 import（见 design E10）
- [x] 4.3 新建 `src/editor/`：CM6 实例装配、markdown 高亮、深色主题
- [x] 4.4 点击文件树某篇 → `read_note` → 内容进缓冲，哈希存为基线
- [x] 4.5 实现自动保存：停止输入 800ms 提交一次 `apply_edits`
- [x] 4.6 实现立即落盘：窗口失焦、切换笔记、关窗前
- [x] 4.7 落盘失败时不切换笔记、不关窗，把原因显示出来（见 design E8）
- [x] 4.8 缓冲无变更时不提交空 ChangeSet
- [ ] 4.9 验证（人工）：打开一篇笔记，敲字，等一秒，`git diff` 只显示自己敲的那处
- [ ] 4.10 验证（人工）：不做编辑就切走，`git diff` 为空

> 本组结束后用户能看见什么：**能真正写笔记了**。这是这一期最要紧的一组，
> 到这里「打开 → 敲字 → 自动存」闭环跑通，后面都是在这之上加渲染。

## 5. 所见即所得渲染

- [x] 5.1 建立行文本 → 解析结果的缓存，打开文件时用 `parse_todo_lines` 批量填满（见 design E4）
- [x] 5.2 缓存未命中的行按原文显示，同时排进异步解析队列；结果回来后 dispatch effect 触发重算
- [x] 5.3 实现「与选区相交的行显示原始语法」：多选区、跨行选中都算（见 design E6）
- [x] 5.4 实现五种 chip 的 decoration：时间、重复、标签、强度，`~id` 完全隐藏且不占宽度
- [x] 5.5 引号包裹的字面量渲染为去掉引号的普通文本，不成 chip
- [x] 5.6 降级标记加轻量警告下划线与悬浮提示，不成 chip、不阻塞编辑
- [x] 5.7 chip 显示文案在前端格式化（`2026-08-14 18:00` → `8月14日 18:00`），且这个结果绝不进文档（见 design E5）
- [x] 5.8 解析链路不可用时退化为纯文本编辑器，编辑与保存全部照常
- [x] 5.10 通用 markdown 渲染：复用 lang-markdown 语法树隐藏 `#` `**` `` ` `` `>` `~~` `[]()`，标题按级别放大、引用加左边条、链接只显示文字
- [x] 5.11 裸 URL / 自动链接不隐藏地址（没有替代文字，藏了等于内容消失）
- [x] 5.14 围栏代码块：块背景 + 左边条 + 首尾圆角，`` ``` `` 隐藏、语言名转小徽标；行内代码加底色。块级样式不随光标进出消失
- [x] 5.15 代码块按语言着色：`@codemirror/language-data` 的 143 种语言注册为按需加载，补齐嵌套语言用的通用 tag 配色
- [x] 5.16 语法树变化也触发 decoration 重算——语言包异步到位、大文件渐进解析都靠这一条，否则首屏那版不完整的树会一直留着
- [x] 5.12 chip 与 markdown 语法共用同一份 replace 占用表，冲突时 chip 优先（CM6 里 replace 重叠会白屏）
- [x] 5.9 验证（人工）：光标进出含标记的行，chip 与原始语法来回切换，`git diff` 全程为空
- [x] 5.13 验证（人工）：标题/粗体/引用/链接在光标离开时渲染、进入时还原，`git diff` 全程为空

> 本组结束后用户能看见什么：Typora 式的效果第一次出现——`~a3f9` 消失了，
> 时间变成了 chip，光标一进去又变回原文。这是 D11/D16 的核心验证点。

## 6. 复选框与 chip 交互

- [x] 6.1 渲染态复选框可点击切换，变更进缓冲后立即落盘（见 design E7）
- [x] 6.2 切换只改复选框字符，该行其余字节不变
- [x] 6.3 时间 chip 可点开修改；已定为独立窗口（见 design Open Questions 的结论）
- [x] 6.4 重复规则、标签、强度 chip 可点开修改
- [x] 6.5 chip 修改产出的文本一律由 0.2 的序列化写回；速记条的选择器路径一并改掉手写字节切片
- [x] 6.6 修改时若该待办尚无 `~id` 且此次设了提醒，分配 ID 并与标记写在同一次缓冲变更里
- [x] 6.8 选择器结果带请求 id 路由：`emit` 是广播，不带 id 会让编辑器点 chip 连速记条一起改
- [ ] 6.7 验证（人工）：点 chip 改时间，`git diff` 只有该行、且只有元数据区变化

> 本组结束后用户能看见什么：chip 从死的变成活的——能点、能改、改完落盘。

## 7. 输入辅助接入 CM6

- [ ] 7.1 写 CM6 侧的 assist 适配层：提供光标屏幕坐标、接收选中结果、dispatch 插入
- [ ] 7.2 复用 `src/assist.ts` 与速记条同一套触发规则，不复制第二份
- [ ] 7.3 Esc 只收弹层，不影响编辑器其他 Esc 行为
- [ ] 7.4 按键处理绝不 await 解析
- [ ] 7.5 `#` 弹层这一期只能新建标签（`list_tags` 依赖未做的索引，须在界面上如实体现，不显示空列表假装没有标签）
- [ ] 7.6 验证（人工）：编辑器里 `- [ ] 交周报 @` 一气呵成，落盘为规范化的行内标记

> 本组结束后用户能看见什么：编辑器里敲 `@` 就弹时间选择器——3.3 的输入辅助
> 从速记条搬到了它真正的主场。

## 8. rebase 与撤销边界

- [ ] 8.1 实现「广播来的 Rust op → 本文档坐标系的 CM6 ChangeSet」转换层（见 design E3）
- [ ] 8.2 维护未确认变更集合；广播回来是自己那批则标记已确认，不重复应用
- [ ] 8.3 基线冲突被拒时：应用磁盘新内容为新基线 → `unconfirmed.map()` 重映射 → 重投
- [ ] 8.4 连续重投失败达上限则作为可见失败上报，缓冲内容不丢
- [ ] 8.5 远端变更用 annotation 标记且 `addToHistory: false`，不进本窗口撤销栈（D20）
- [ ] 8.6 验证（人工）：编辑器打开 `inbox.md` 并敲字未落盘时，用速记条往 `inbox.md` 追加一条，两边内容都不丢
- [ ] 8.7 验证（人工）：接上一条，之后连按 Ctrl+Z 撤不掉速记条那次追加

> 本组结束后用户能看见什么：正常使用下看不见——它保证的是「不该发生的事没发生」。
> 8.6 / 8.7 是唯一能证明这组真做了的手段，必须实跑。

## 9. 收尾

- [ ] 9.1 `cargo clippy` 无警告，`cargo test` 全绿
- [ ] 9.2 `npm run build` 与 `cargo build` 均通过
- [ ] 9.3 确认构建产物里速记条的 chunk 未引入 CM6（见 design E10）
- [ ] 9.4 验证（人工）：回归热键延迟测量，p95 仍在 200ms 内（NFR-2）
- [ ] 9.5 验证（人工）：在 VS Code 里手写畸形待办行与超长行，编辑器不崩、不改文件
- [ ] 9.6 更新 README 的已完成与未完成两节，明说本期不含文件监听、表格、图片内联


